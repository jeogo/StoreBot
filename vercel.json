{
  "version": 2,
  "builds": [
    {
      "src": "src/**/*.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["src/**/*.ts"]
      }
    }
  ],
  "routes": [
    {
      "src": "/bot(.*)",
      "dest": "/src/server.ts"
    }
  ]
}
